node_modules/
dist/
.DS_Store
*.log
.vite/

# Claude Code: keep only launch.json, ignore everything else
.claude/*
!.claude/launch.json

# FUSE leftover files from the bind mount
.fuse_hidden*

# Stray office documents dropped at the root by the user
*.odt
*.docx
*.doc
.~lock.*#

# Stray PNGs at the root (the website assets live in public/ only)
/*.png
!public/*.png
